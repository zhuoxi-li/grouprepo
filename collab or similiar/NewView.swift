import SwiftUI

struct NewSwift: View {
    var body: some View {
        VStack {
            Text("Hello, World!")
                .foregroundColor(.red)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
